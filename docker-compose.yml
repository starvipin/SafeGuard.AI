# Local container configuration; docker compose up --build builds and starts the application.
services:
  # Build the app service from the Dockerfile in the project root.
  app:
    build: .
    # The mapping is host:container; requests to localhost:5000 reach port 5000 inside the container.
    ports:
      - "5000:5000"
